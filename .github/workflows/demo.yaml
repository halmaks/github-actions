name: Demo Workflow
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Pring to console
        run: echo "hello github actions"
      - name: Print a few lines
        run: |
          echo "First line"
          echo "Second line"